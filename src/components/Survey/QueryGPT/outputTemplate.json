{
  "stacks": [
    {
      "name": "placeholder", // Name of the tech stack
      "database": "placeholder",
      "backend": "placeholder",
      "frontend": {
        "primary": "placeholder",
        "secondaries": ["placeholder1", "placeholder2", "..."] // List which might contain items like "redux", "sass", "typescript", etc. but might also be empty
      },
      "pros": ["placeholder1", "placeholder2", "..."], // Same rules as the secondaries list
      "cons": ["placeholder1", "..."], // Same rules as above
      "description": "placeholder" // Argumentation for and against this tech stack, reasoning why it is good or bad think step-by-step
    },
    {
      "name": "placeholder",
      "database": "placeholder",
      "backend": "placeholder",
      "frontend": {
        "primary": "placeholder",
        "secondaries": ["placeholder1", "..."] // This has the same rules as the previous secondaries list
      },
      "pros": ["placeholder1"], // Same rules as before
      "cons": ["placeholder1"], // Same rules as before
      "description": "placeholder" // Be sure to replace the string with the actual reasons/description of the tech stack
    },
    {
      "name": "placeholder",
      "database": "placeholder",
      "backend": "placeholder",
      "frontend": {
        "primary": "placeholder",
        "secondaries": ["placeholder1", "placeholder2"] // This has the same rules as the previous secondaries list
      },
      "pros": ["placeholder1"], // Same rules as before
      "cons": ["placeholder1"], // Same rules as before
      "description": "placeholder" // Be sure to replace the string with the actual reasons/description of the tech stack
    }
  ]
}
